<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-auto">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">MAKERS URL Shortner</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to "Nova Url", "/new", :class => "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Minhas URL's", "/url", :class => "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>

    <div class="align-items-end me-5 text-end">
      <% if logged_in? %>
        <div class="row">
          <h5 class="h5"><%= current_user.email %></h5>
          <%= button_to "Log Out", logout_path, method: :get, :class => "btn btn-primary btn-sm" %>
        </div>
      <% else %>
        <% if !current_page?(login_path) %>
          <%= button_to "Log In", login_path, method: :get, :class => "btn btn-primary btn-sm" %>
        <% end %>
      <% end %>
    </div>
  </div>
</nav>
